apiVersion: v1
kind: Service
metadata:
  name: api-service  # Changed Name
spec:    # New Type
  selector:
    app: synergychat-api  # Changed Selector to match API pods
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080  # The specific "hole" we are drilling
